apiVersion: v1
kind: Service
metadata:
  name: app-admin
  labels:
    app: app-admin
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: app-admin